$("#ModalTambahKeg").on('hide.bs.modal', function (e) {
    $(this).find('form')[0].reset();
   
});

$("#ModalTambahIndikator").on('hide.bs.modal', function (e) {
    $(this).find('form')[0].reset();
   
});
